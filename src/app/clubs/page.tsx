"use client";
import { useEffect, useState } from "react";
import Link from "next/link";
import { Calendar, Users } from "lucide-react";
import api from "@/lib/api";
import PublicShell from "@/components/public/public-shell";
type Club = { _id: string; name: string; description: string; memberCount: number; meetingDate?: string; currentBook?: { title: string; author: string } };
export default function PublicClubsPage() { const [clubs, setClubs] = useState<Club[]>([]); const [error,setError]=useState(""); useEffect(() => { api.get("/clubs").then((r) => setClubs(r.data.clubs || [])).catch(()=>setError("Book clubs are temporarily unavailable.")); }, []); return <PublicShell><section className="mx-auto max-w-7xl px-6 py-14"><p className="font-bold uppercase tracking-[.2em] text-violet-600">Community</p><h1 className="mt-3 text-5xl font-black">Join a book club</h1><p className="mt-4 max-w-2xl text-slate-500">Read together, meet regularly, and share what the story means to you.</p>{error&&<p className="mt-6 rounded-xl bg-amber-50 p-4 text-amber-800">{error}</p>}<div className="mt-10 grid gap-6 md:grid-cols-2 xl:grid-cols-3">{clubs.map((club) => <Link href={`/clubs/${club._id}`} key={club._id} className="rounded-3xl border bg-white p-6 shadow-sm transition hover:-translate-y-1 hover:shadow-xl"><p className="text-xs font-bold uppercase text-violet-600">Currently reading</p><h2 className="mt-3 text-2xl font-black">{club.name}</h2><p className="mt-2 min-h-12 text-sm text-slate-500">{club.description}</p><div className="mt-6 rounded-2xl bg-slate-50 p-4"><p className="font-semibold">{club.currentBook?.title || "Book selection coming soon"}</p><p className="text-sm text-slate-500">{club.currentBook?.author}</p></div><div className="mt-5 flex justify-between text-sm text-slate-500"><span className="flex gap-2"><Users size={16} />{club.memberCount}</span><span className="flex gap-2"><Calendar size={16} />{club.meetingDate ? new Date(club.meetingDate).toLocaleDateString() : "TBA"}</span></div></Link>)}</div></section></PublicShell>; }
