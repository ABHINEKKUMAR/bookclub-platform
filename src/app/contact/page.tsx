"use client";import{useState}from"react";import api from "@/lib/api";import InfoPage from "@/components/public/info-page";export default function Contact(){const[sent,setSent]=useState(false);const[form,setForm]=useState({name:"",email:"",message:""});return <InfoPage eyebrow="Support" title="Contact us">{sent?<p className="rounded-xl bg-emerald-50 p-4 text-emerald-700">Thanks. Your message has been received.</p>:<form onSubmit={async e=>{e.preventDefault();await api.post("/contact",form);setSent(true)}} className="grid gap-4"><input required placeholder="Name" value={form.name} onChange={e=>setForm({...form,name:e.target.value})} className="rounded-xl border bg-white p-3"/><input required type="email" placeholder="Email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} className="rounded-xl border bg-white p-3"/><textarea required placeholder="How can we help?" value={form.message} onChange={e=>setForm({...form,message:e.target.value})} className="min-h-36 rounded-xl border bg-white p-3"/><button className="rounded-xl bg-blue-600 px-5 py-3 font-bold text-white">Send message</button></form>}</InfoPage>}
