"use client";import{useState}from"react";import Link from"next/link";import api from "@/lib/api";
export default function ForgotPassword(){const[email,setEmail]=useState("");const[message,setMessage]=useState("");return <main className="flex min-h-screen items-center justify-center bg-slate-100 p-6"><form onSubmit={async e=>{e.preventDefault();const r=await api.post("/auth/forgot-password",{email});setMessage(r.data.resetToken?`${r.data.message}. Development token: ${r.data.resetToken}`:r.data.message)}} className="w-full max-w-md rounded-3xl bg-white p-8 shadow-xl"><h1 className="text-3xl font-black">Reset your password</h1><p className="mt-3 text-slate-500">Enter your email to request reset instructions.</p><input required type="email" value={email} onChange={e=>setEmail(e.target.value)} className="mt-6 w-full rounded-xl border p-3" placeholder="you@example.com"/><button className="mt-4 w-full rounded-xl bg-blue-600 p-3 font-bold text-white">Request reset</button>{message&&<p className="mt-4 break-words rounded-xl bg-blue-50 p-3 text-sm text-blue-700">{message}</p>}<Link href="/login" className="mt-5 block text-center text-sm font-bold text-blue-600">Back to login</Link></form></main>}
