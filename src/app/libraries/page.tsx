"use client";
import {useEffect,useState} from "react";import {BookOpen,MapPin,Users} from "lucide-react";import api from "@/lib/api";import PublicShell from "@/components/public/public-shell";
type Library={_id:string;name:string;location:string;books:number;members:number;status:string};
export default function PublicLibrariesPage(){const[items,setItems]=useState<Library[]>([]);const[error,setError]=useState("");useEffect(()=>{api.get("/libraries").then(r=>setItems(r.data||[])).catch(()=>setError("Libraries are temporarily unavailable."))},[]);return <PublicShell><section className="mx-auto max-w-7xl px-6 py-14"><p className="font-bold uppercase tracking-[.2em] text-blue-600">Library network</p><h1 className="mt-3 text-5xl font-black">Find a connected library</h1>{error&&<p className="mt-6 rounded-xl bg-amber-50 p-4 text-amber-800">{error}</p>}<div className="mt-10 grid gap-5 md:grid-cols-2 xl:grid-cols-3">{items.map(lib=><article key={lib._id} className="rounded-3xl border bg-white p-6 shadow-sm"><span className="rounded-full bg-emerald-50 px-3 py-1 text-xs font-bold text-emerald-700">{lib.status}</span><h2 className="mt-5 text-2xl font-black">{lib.name}</h2><p className="mt-2 flex gap-2 text-slate-500"><MapPin size={18}/>{lib.location}</p><div className="mt-6 flex gap-5 text-sm text-slate-500"><span className="flex gap-2"><BookOpen size={16}/>{lib.books} books</span><span className="flex gap-2"><Users size={16}/>{lib.members} members</span></div></article>)}</div></section></PublicShell>}
