"use client";import{useState}from"react";import{useParams}from"next/navigation";import Link from"next/link";import api from "@/lib/api";
export default function ResetPassword(){const{token}=useParams<{token:string}>();const[password,setPassword]=useState("");const[message,setMessage]=useState("");return <main className="flex min-h-screen items-center justify-center bg-slate-100 p-6"><form onSubmit={async e=>{e.preventDefault();const r=await api.post(`/auth/reset-password/${token}`,{password});setMessage(r.data.message)}} className="w-full max-w-md rounded-3xl bg-white p-8 shadow-xl"><h1 className="text-3xl font-black">Choose a new password</h1><input required minLength={8} type="password" value={password} onChange={e=>setPassword(e.target.value)} className="mt-6 w-full rounded-xl border p-3" placeholder="At least 8 characters"/><button className="mt-4 w-full rounded-xl bg-blue-600 p-3 font-bold text-white">Update password</button>{message&&<p className="mt-4 text-emerald-600">{message}</p>}<Link href="/login" className="mt-5 block text-center text-sm font-bold text-blue-600">Login</Link></form></main>}
